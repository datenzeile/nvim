-- ABOUTME: Auto-pairs plugin for automatic bracket/quote closing (disabled).
-- ABOUTME: Available for re-enabling by setting enabled = true.

return {
  'windwp/nvim-autopairs',
  enabled = false,
  event = 'InsertEnter',
  opts = {},
}
